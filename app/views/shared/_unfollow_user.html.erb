<%= form_for(current_user.relationships.find_by_followed_id(user.id),
             html: { method: :delete },
             remote: true) do |f| %>
<%= link_to 'Unfollow', "#", :onclick=>"$('#form_index_#{user.id}').find('form').submit()", :style => "color:#08c; font-size:0.8em" %>
<% end %>