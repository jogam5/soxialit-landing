<%= form_for(current_user.relationships.find_by_followed_id(user.id),
             html: { method: :delete },
             remote: true) do |f| %>
<%= link_to 'Unfollow', "#", :onclick=>"$('.edit_relationship').submit()", :style => "color:white", 
		:id => "user_unfollow_button" %>
<% end %>