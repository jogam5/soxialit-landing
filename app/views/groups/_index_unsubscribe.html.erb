<%= form_for(current_user.memberships.find_by_group_id(group.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= link_to 'Cancelar Suscripcion', "#", :onclick=>"$('#form_index_#{group.id}').find('form').submit()",
			:id => "user_unfollow_button" %>
<% end %>