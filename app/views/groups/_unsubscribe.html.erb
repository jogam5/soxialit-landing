<%= form_for(current_user.memberships.find_by_group_id(@group),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Cancelar Suscripcion", :id => "user_unfollow_button" %>
<% end %>