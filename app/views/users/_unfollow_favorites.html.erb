<%= form_for(current_user.relationships.find_by_followed_id(@user),
             html: { method: :delete },
             remote: true) do |f| %>
<%= link_to 'Unfollow', "#", :onclick=>"$('.edit_relationship').submit()", :style => "color:#08c; font-size:0.8em" %>
<% end %>