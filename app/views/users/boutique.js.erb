$('#user_roles').html("<%= escape_javascript(render :partial => 'user_boutique', :object => @boutiques) %>");
$(function(){
	$('#user_follow_button').bind('click', function(e){
	  e.preventDefault(); // Prevent link from following its href
	});
});
