$('#user_roles').html("<%= escape_javascript(render :partial => 'user_fashionlover', :object => @fashionlovers) %>");
$(function(){
	$('#user_unfollow_button').bind('click', function(e){
	  e.preventDefault(); // Prevent link from following its href
	});
});


