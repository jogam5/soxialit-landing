/*$(document).ready(function(){
	$('#products').live({
		mouseenter: 
		function(){
			$('.reputation_'+ $(this).data('id')).fadeIn();
			$('.votacion_'+ $(this).data('id')).fadeIn();

			},
		mouseleave:
			function (){
				$('.reputation_'+ $(this).data('id')).hide();
				$('.votacion_'+ $(this).data('id')).hide();

			}
	  });

});
*/

$(function(){
    $('#profile_nav_bar a').click(function(){
        $('#profile_nav_bar .active').removeClass('active'); // remove the class from the currently selected
        $(this).addClass('active'); // add the class to the newly clicked link
    });
});

/*
$(document).ready(function(){
	$('#image_hover').live({
		mouseenter: 
		function(e){
			e.preventDefault();
			$(this).find('.votacion').fadeIn();
			},
		mouseleave:
		function (e){
			e.preventDefault();
			$(this).find('.votacion').fadeOut();
		}
	});


});


$(document).ready(function(){
	$('#favorites_image').live({
		mouseenter: 
		function(e){
			e.preventDefault();
			$(this).find('.votacion').fadeIn();
			},
		mouseleave:
		function (e){
			e.preventDefault();
			$(this).find('.votacion').fadeOut();
		}
	});
	
	
});
*/


/* Masonry en index favoritos */
window.onload = function() {
  var wall = new Masonry( document.getElementById('contenedor') );
};


$(document).ready(function(){
	$('.link_user_index').live('click', function(){
		$('.users_follow span').find('a').css('color', '#666');
		$(this).css('color', '#DD390D');
	});
});
